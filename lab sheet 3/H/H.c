#include<stdio.h>
#include<stdlib.h>

void toGray(long long int n){
    int flag =0 , pre = 0 , v=0;
    while(n){
        v= n%2;
        n/=2;
        if(!flag)
    }
}

int main(){
    long long int t , n ;
    char q;
    while(t--){
        scanf("%c ",&c);
        if(c=='G'){
            scanf("%lld",&n);
            toGray(n);
        }
    }
}